<div class="page-header text-left">
  <h1>Profile</h1>
</div>

  <% if @user.errors.present? %>
    <% @user.errors.full_messages.each do |message| %>
      <p class="text-danger"><%= message %></p>
      <% end %>
  <% end %>

<%= render 'form', button_name: "Update User" %>
